{
  "devDependencies": {
    "@types/node": "^22.15.18",
    "@vitest/coverage-v8": "^3.1.3",
    "prettier": "3.5.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  },
  "dependencies": {
    "redis": "^5.0.1"
  },
  "engines": {
    "node": "22.15.1",
    "npm": "10.9.2"
  },
  "main": "app.mts",
  "name": "redis-iterative-consume",
  "scripts": {
    "node:app": "node --inspect --loader ts-node/esm app.mts",
    "node:version": "node -v; npm -v",
    "redis:cli": "redis-cli",
    "redis:server": "redis-server",
    "redis:server:tail": "tail -fn +0 /var/log/redis/redis-server.log",
    "redis:server:version": "redis-server -v",
    "tsc:compile": "tsc -p tsconfig.json",
    "tsc:config": "tsc --showConfig",
    "tsc:version": "tsc -v",
    "vitest": "vitest",
    "vitest:coverage": "vitest run --coverage"
  },
  "type": "module",
  "version": "0.1.0"
}
